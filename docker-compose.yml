services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nestdb
    networks:
      - app-network
    ports:
      - '5432:5432'
networks:
  app-network: